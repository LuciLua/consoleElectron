// const execFileSync = require('child_process').execFileSync;

function openValorant() {
    // const stdout = execFileSync('node', ['--version']);
    // console.log(stdout);
    // alert(stdout)
    holdMsg.insertAdjacentHTML("afterbegin", "<div class='ls'> <p class='go'>  THIS IS VALORANT POF POF POF NOW YOU ARE DEAD </p> </div>")
}