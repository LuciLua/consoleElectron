import styles from "./Header.css"

function Header(){
    return(
        <div class={styles.header}>
            <p>
                header
            </p>
        </div>
    )
}

export default Header